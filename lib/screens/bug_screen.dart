import 'package:flutter/material.dart';

class BugScreen extends StatelessWidget {
  const BugScreen({Key? key}) : super(key: key);
  static const routeName = '/bug-screen';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
